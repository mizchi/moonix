// Generated by moon-component
// Import module: wasi:clocks/wall-clock@0.2.9

///|
pub(all) struct Datetime {
  seconds : UInt64
  nanoseconds : UInt
} derive(Show, Eq)

///| Low-level FFI import
fn ffi_now(retptr : Int) -> Int = "wasi:clocks/wall-clock@0.2.9" "now"

///| High-level wrapper with canonical ABI conversion
pub fn now() -> Datetime {
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_now(retptr)
  ({ seconds: @cabi.cabi_read_i64(retptr).reinterpret_as_uint64(), nanoseconds: @cabi.cabi_read_i32(retptr + 8).reinterpret_as_uint() } : Datetime)
}

///| Low-level FFI import
fn ffi_resolution(retptr : Int) -> Int = "wasi:clocks/wall-clock@0.2.9" "resolution"

///| High-level wrapper with canonical ABI conversion
pub fn resolution() -> Datetime {
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_resolution(retptr)
  ({ seconds: @cabi.cabi_read_i64(retptr).reinterpret_as_uint64(), nanoseconds: @cabi.cabi_read_i32(retptr + 8).reinterpret_as_uint() } : Datetime)
}

